///| Ultra minimal - just print to console

fn main {
  println("OneBit-TUI MoonBit Demo")
  println("=======================")
  println("")
  println("Core package has 78 compilation errors")
  println("But we can at least run this!")
  println("")
  println("Next steps:")
  println("1. Fix handler type mismatches")
  println("2. Complete missing component implementations")  
  println("3. Sync with upstream OpenTUI")
}